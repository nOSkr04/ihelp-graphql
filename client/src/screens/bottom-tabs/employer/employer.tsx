import { StyleSheet, Text, View } from 'react-native'
import React, { memo } from 'react'

const EmployerTab = memo(() => {
    return (
      <View>
        <Text>EmployerTab</Text>
      </View>
    )
  })

  EmployerTab.displayName="EmployerTab"

export {EmployerTab}

const styles = StyleSheet.create({})